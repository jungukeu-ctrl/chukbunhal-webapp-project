# 축분할 코칭 보드 - 고도화 PLAN

## 현재 상태
- 버전: 최종 완결판 (하이픈/물결 reps 범위 지원, Fallback 로직)
- 문제: 매 버튼 클릭마다 서버 호출 (느림), RPE/reps 메모 미활용, 추천 로직 단순

---

## 고도화 목표

### 1. 성능: 전체 데이터 1회 로드 아키텍처
- 앱 시작 시 `getAllData()` 1회만 호출
- 모든 루틴 데이터 + 전체 기록 + 대체운동 맵을 JS 메모리에 저장
- 프로그램/주/일 변경 → 서버 호출 없이 JS 필터링 (즉시)
- 종목 교체 → 서버 호출 없이 JS 재계산 (즉시)

### 2. 스마트 추천: 경험적 점진적 과부하 기반
**메모 파싱**
- 형식: `rpe7 reps3~6` / `rpe7 reps8-12` / `rpe7 reps8` + 뒤에 잡음 텍스트 가능
- 정규식: `/rpe(\d+(?:\.\d+)?)\s+reps(\d+)(?:[~-](\d+))?/i`

**같은 날짜 Top Set 선정**
- 같은 종목 + 같은 rpe + 같은 reps범위인 세트 중 최고 무게 선택

**매칭 우선순위 (P1 → P4)**
- P1: reps범위 일치 + RPE 일치 → 완전 일치
- P2: reps범위만 일치 → reps가 더 중요
- P3: RPE만 일치 → RPE만 맞는 경우
- P4: 둘 다 불일치 → 해당 종목 최근 기록 fallback

**추천 표시**
- 마지막 무게 그대로 표시 (자동 증가 없음, 사용자 판단)
- 매칭 기준 명시 (P1~P4 레이블)
- 최근 3세션 추이 표시

### 3. UI: 완료 체크
- 카드별 완료 체크 버튼
- 완료 시 시각적 구분 (초록 border)
- 헤더에 진행률 표시 (예: 3 / 6 완료)

---

## 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| Code.js | `getAllData()` 함수 추가 (기존 함수 유지) |
| index.html | 전면 재작성 (JS 분석 엔진 + UI 개선) |
| PLAN.md | 이 파일 |

**변경 없는 것**
- `importLatestCSV()` — CSV 동기화 로직 그대로 유지
- `onOpen()` — 메뉴 그대로 유지
- Firebase — 해당 없음

---

## ⚠️ 코드 작성 전 필수 영향도 검증 체크리스트

각 Step 코드 작성 전 반드시 아래 항목 검증 후 보고:

### A. 기존 기능 목록 (건드리면 안 되는 것)
| 기능 | 관련 함수/코드 위치 | 영향 여부 |
|------|-----------------|---------|
| 프로그램 선택 → 주 목록 갱신 | `loadWeeks()` | 확인 필요 |
| 주 선택 → 일 목록 갱신 | `loadDays()` | 확인 필요 |
| 일 선택 → 루틴 카드 표시 | `loadRoutine()` | 확인 필요 |
| 종목 교체 버튼 | `swapExercise()` | 확인 필요 |
| 세트 기록 저장 | `saveSet()` | 확인 필요 |
| CSV 동기화 | `importLatestCSV()` | 변경 없음 |
| 메뉴 | `onOpen()` | 변경 없음 |

### B. 검증 질문 (코드 작성 전 답변)
1. 이번 Step이 수정하는 함수/변수 목록은?
2. 위 표의 기존 기능 중 직접/간접적으로 영향받는 것은?
3. 데이터 흐름이 바뀌는가? (서버 응답 구조, 변수명 등)
4. 기존 코드를 삭제/덮어쓰는가, 아니면 추가만 하는가?
5. 롤백 방법은? (어느 줄을 되돌리면 복원되는가)

---

## 작업 단계

- [x] Step 1: Code.js에 `getAllData()` 함수 추가
- [x] Step 2: index.html — 초기 로드 + JS 데이터 캐싱 구조
- [x] Step 3: index.html — 메모 파싱 함수
- [x] Step 4: index.html — P1~P4 매칭 + Top Set 선정 로직
- [x] Step 5: index.html — 추이 3세션 표시 UI
- [x] Step 6: index.html — 완료 체크 UI
- [x] Step 7: index.html — 종목 교체 JS 처리 (서버 호출 제거)

---

## 업데이트 이력
- 2026-03-23: 최초 작성
- 2026-03-30: 코드 작성 전 영향도 검증 체크리스트 추가
- 2026-03-30: 전체 고도화 작업 완료 (Step 1~7)
